#
#	.kshpwd - local stuff to abbreviate PWD...
#
#ident	"@(#)HOME:.kshpwd	2.2	94/11/18 00:18:45 (woods)"

if [ "$PWD" = "$HOME" ] ; then
	PWD='~'
fi
unalias cd
alias cd='_cd'
function _cd
{
	\cd $*
	case "$PWD" in
	${HOME})
		PWD='~'
		;;
	`dirname ${HOME}`/*)
		PWD='~'"${PWD#`dirname $HOME`/}"
		;;
	/home/*)
		PWD='~'"${PWD#/home/}"
		;;
	/apc/usr/web/webadmin/*)
		PWD='~'"${PWD#/apc/usr/web/webadmin/}"
		;;
	esac
	if [ "`ismpx`" = yes ] ; then
		eval myxban -l "\"$MYXBAN_L\""
	fi
	if [ "$TERM" = "xterm" ] ; then
		setban
	fi
}
